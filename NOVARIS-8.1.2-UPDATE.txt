Novaris 8.1.2 - Proxy Logo Fix

Fixed all three old F marks in the tabbed proxy:
- Top-left proxy toolbar brand button
- New-tab start-page logo
- New-tab favicon fallback

All three now use:
  /assets/media/brand/novaris-mark.svg

Double checks:
- Every first-level HTML title remains exactly "Home".
- No FuzzTheHuzz / Fuzz Cloud / Fuzz AI / Fuzz Control / Fuzz Home
  user-facing branding was found in the current HTML/JS frontend scan.
- Internal compatibility names such as window.FuzzProxy and old storage
  keys were intentionally preserved because renaming them could break
  existing settings or proxy behavior.
- npm run check passes.
